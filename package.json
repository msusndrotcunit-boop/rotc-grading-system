{
  "name": "rotc-grading-system",
  "version": "2.4.7",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix server && cd client && npm install && npm run build",
    "build": "echo 'Build handled by postinstall'",
    "start": "cd server && npm install && node server.js"
  }
}