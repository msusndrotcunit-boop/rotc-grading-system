services:
  - type: web
    name: rotc-grading-system
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    autoDeploy: false
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
